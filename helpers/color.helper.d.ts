export declare class MapleColorHelper {
    static getContrastColor(hexColor: string): string;
    static alpha2hex(alpha: string): string;
    static hex8(hex: string, opacity?: number): string;
    static hex2Short(hex: string): string;
    static generateAlphaColors(color: any): string;
}
